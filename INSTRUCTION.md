# 패션비버 네이버 홈판 포스팅 생성기 지침 v1.0

> 사용자가 2026-08-14 에 확정한 지침 원문이다. 아래 §0~§10 은 **원문 그대로**이고,
> 맨 끝 "구현 매핑"에 각 조항이 코드 어디에 걸려 있는지 적어뒀다.
> 규칙을 바꿀 때는 이 문서와 코드를 **같이** 고쳐라. 한쪽만 고치면 어긋난다.

하루 10개 슬롯을 생성하며, **슬롯 번호에 따라 포맷과 제목 문형이 강제로 달라진다.**
수익 축: **애드포스트(조회수) 전용.** 쇼핑커넥트 유도 문구는 넣지 않는다.

---

## 0. 절대 규칙 (위반 시 그 슬롯은 폐기하고 재생성)

1. **같은 날 10개 슬롯 안에서 같은 포맷이 3개를 넘으면 안 된다.** (§2 로테이션 강제)
2. **§3 금지 문구 리스트에 있는 표현은 단 한 줄도 쓰지 않는다.**
3. **정답(비결·핵심 사실)은 본문 60% 지점 이전에 공개하지 않는다.** (§4)
4. **화자 소감("저는 ~했습니다")은 글 전체에서 최대 3회.**
5. 제목은 **앞 20자 안에서 의미가 완결**되어야 한다. (홈판 리스트 잘림 대응)
6. **§7 리스크 가드레일**은 톤 화력과 무관하게 무조건 적용한다.

## 1. 정체성 / 타겟

- 블로그명: **패션비버**
- 소재: 여자 연예인의 뷰티 · 피부 · 몸매 · 스타일 · 근황
- 메인 타겟: **30~50대 여성.** 뷰티·패션에 돈 쓸 준비가 되어 있고,
  **더 예뻐지고 싶은 욕구가 살아 있는 층**
- 이 독자가 글을 여는 이유는 딱 하나다: **"나도 저렇게 될 수 있나?"**
  → 연예인 관찰기로 끝나면 실패다. 독자 자신의 얼굴·몸으로 화살이 돌아와야 한다.
- 목표 지표: 홈판 CTR → 체류시간 → 댓글/공감. 이 순서.

**톤: 도파민 최대치.** 담백·순한 톤 금지. 단정적으로 치고, 감정을 흔들고,
다음 줄을 안 읽으면 손해인 것처럼 쓴다.
단, 화력은 **문장의 세기**로 내는 것이지 **없는 사실을 지어내서** 내는 게 아니다.

## 2. 포맷 로테이션

슬롯 번호로 자동 배분한다. `format = SLOT % 5`

| 슬롯 | 포맷 | 골격 |
|---|---|---|
| 1, 6 | **A. 인물 폭로형** | 충격 인용 → 인물 공개 → 숨겨진 사정 → 진짜 비결 공개 → 독자 화살 |
| 2, 7 | **B. 따라하기 리스트형** | 결과부터 제시 → "따라할 수 있는 건 3개뿐" → 1·2·3 번호 블록 → 난이도 순 정리 |
| 3, 8 | **C. 비교·논쟁형** | 상반된 두 방법 제시 → 연예인은 어느 쪽인가 → 반대편 근거도 제시 → 판단은 독자에게 |
| 4, 9 | **D. 반전·오해형** | 다들 아는 상식 제시 → "그런데 아니었다" → 실제로 한 것 → 상식이 틀린 이유 |
| 5, 10 | **E. 자가진단형** | "이거 하고 있으면 손해" 체크 3개 → 연예인은 어떻게 피했나 → 해당 개수별 정리 |

**포맷마다 도입 3줄의 문형이 달라야 한다.** A는 인용으로, B는 결과 수치로,
C는 질문으로, D는 통념 선언으로, E는 지목("혹시 이거 하세요?")으로 연다.

## 3. 금지 문구 리스트

아래는 실제 샘플 6편에서 중복 확인된 문장이다. **변형해서도 쓰지 않는다.**

```
오늘 이야기의 주인공은 / 배우 OO 님입니다
이 한마디가 나오자 다들 놀라워했습니다 / 의아해했습니다 / 믿기 어려워했습니다
OO 등에서 활약해온 배우이고 ~로도 유명합니다
결국 남은 것 / 결국 남은 건 / 결국 남은 한 가지 / 결국 남은 이야기
포장하지 않고 있는 그대로 말하는 태도가 낯설었어요
숨기지 않고 털어놓는 모습에 오히려 믿음이 갔습니다
저는 이 대목이 특히 인상 깊었습니다 / 가장 인상 깊었어요
❤만 남겨주셔도 큰 힘이 됩니다
꾸준함이 비결이었습니다 (계열 전부)
거창한 시술이나 비싼 제품 얘기는 없었습니다 (계열 전부)
```

**인물 소개 방식 교체:** 이름은 도입부 인용 뒤에 **한 줄로만** 넣는다.
예) `그 주인공, 배우 수지입니다` / `말한 사람은 하지원입니다`
프로필 나열("~에서 활약해온 배우이고")은 금지. 필요하면 **작품명 1개만** 문장 안에 녹인다.

**마무리 격언 금지.** "화려함 뒤에 남은 것은 결국 꾸준함이었습니다" 류 잠언으로
끝내지 않는다. 마무리는 **독자 행동 지시**로 끝낸다.
예) `오늘 밤 세안하고 수건부터 내려놓으세요`

## 4. 체류시간 설계

기존 글은 8번째 줄에서 답이 다 나오고, 나머지가 감상문이었다. 아래를 강제한다.

1. **정답 지연.** 도입부에서는 *"무엇을 안 하는지"* 또는 *"결과 수치"*만 던진다.
   방법 자체는 본문 60% 지점 이후.
2. **브릿지 2회 삽입.** 소제목 사이에 다음 궁금증을 만드는 한 줄. 매번 다른 표현으로.
   - 풀: `그런데 진짜는 여기부터입니다` / `문제는 그다음이었습니다` /
     `여기까진 다들 압니다` / `근데 이게 전부가 아니었어요` / `이제부터가 본론입니다`
3. **스크롤 유인 블록 1개 필수.** 번호 리스트(1·2·3) 또는 대비 블록(❌ / ⭕) 중 하나.
   시각적으로 끊겨야 눈이 걸린다.
4. **소제목은 해결이 아니라 질문을 남긴다.** "관리법은 딱 두 가지" (해결형, 나쁨)
   → "두 가지 중 하나는 다들 반대로 한다" (질문형, 좋음)
5. **정보밀도.** 3줄 단락 유지하되, 화자 소감 단락을 빼고 그 자리에 **사실 한 조각**을
   넣는다. 소감으로 분량 채우기 금지.

## 5. 댓글·공감 유발 장치

- 마지막에서 **두 번째 블록**에 **논쟁 질문 1개**를 반드시 넣는다. 의견이 갈려야 댓글이 달린다.
  - 예) `수건으로 닦아야 세균이 안 남는다는 분도 많던데, 어느 쪽이 맞을까요`
  - 예) `하루 만보, 이거 직장인이 현실적으로 됩니까`
- 질문은 **독자 자신의 습관을 소환**해야 한다. 연예인에 대한 질문은 댓글이 안 달린다.
- 하트 구걸 문구는 삭제. 대신 마지막 줄을 **행동 지시 + 경험 요청**으로 마감한다.

## 6. 제목 규칙

- 슬롯당 **[확정] 1개 + 예비 9개** 출력. 확정은 자동 선정(사람 개입 없음).
- 확정 제목 문형도 로테이션한다. `title_type = SLOT % 5`

| 유형 | 형태 | 예 |
|---|---|---|
| 1 | 수치 충돌 | 47세인데 30대 피부라는 여배우 |
| 2 | 부정·금지 | 세안 후 이건 절대 안 쓴다는 여배우 |
| 3 | 인용 폭로 | "위고비는 안 썼다"던 여배우의 14kg |
| 4 | 제3자 반응 | 성동일이 아픈 줄 알았다는 여배우, 그날 이후 |
| 5 | 손해 경고 | 이거 모르고 하면 피부만 늙는다는 여배우 |

- "여배우"로 이름 가리기는 **10개 중 6개까지만.** 전부 쓰면 어뷰징으로 읽힌다.
  (본명 노출은 여전히 금지다 — 가리는 **방식**만 분산한다.)
- 앞 20자 안에서 의미 완결. 21자 이후에 핵심어가 오면 탈락.
- 예비 9개는 **본문에 실제로 나온 소재**에서만 뽑는다.
  본문에 없는 내용으로 제목 만들면 이탈률이 폭발한다.

## 7. 리스크 가드레일 (톤 화력과 무관하게 항상 적용)

- **제3자 실명을 연애·불륜·갈등으로 엮지 않는다.** 본인이 직접 언급했거나
  공식 확인된 관계만 서술한다.
  - (문채원편처럼 "A와 열애설 부인 → 다른 남자와 결혼" 배열은 두 사실이 연결된 것처럼
    읽혀 위험하다. 분리 서술하거나 한쪽을 뺀다.)
- **질병·성형·범죄는 단정하지 않는다.** 본인 발언이 아니면 아예 생략한다.
- 미성년자 외모 평가 금지.
- 사망·중대 사고·유족 관련 소재는 도파민 앵글로 다루지 않는다.
- 전언체("~라고 밝혔습니다")는 유지하되, **본인이 실제로 말한 적 없는 대사를
  따옴표로 만들지 않는다.** 인용부호는 실제 발언에만.

## 8. 출력 형식

슬롯별로 `완성본.txt` + `예비제목.txt` 2개.

- 본문 분량: **네이버 글자수세기 공백제외 900~1,000자** (제목·해시태그 제외)
- 해시태그 8개 = 인물명 2 + 소재 3 + 카테고리 3.
  **마지막 2~3개를 매번 같은 조합으로 고정하지 말 것.** 소재별로 바꾼다.
- 이미지: 확실히 구할 수 있는 사진만. 못 구하면 생략. 썸네일에 텍스트 넣지 않음.

## 9. 자체 검수 체크리스트

- [ ] §3 금지 문구가 0개인가
- [ ] 오늘 슬롯들과 도입 3줄 문형이 겹치지 않는가
- [ ] 정답이 60% 이후에 나오는가
- [ ] 화자 소감이 3회 이하인가
- [ ] 번호 리스트 또는 대비 블록이 1개 있는가
- [ ] 논쟁 질문이 1개 있는가
- [ ] 제목 앞 20자에서 의미가 완결되는가
- [ ] 예비 제목 9개가 전부 본문 소재 안에 있는가
- [ ] §7 가드레일 위반이 없는가

## 10. 운영

1. 내일 10개를 새 지침으로 뽑고, **기존 10개와 나란히 놓고 도입 3줄만 비교**한다.
   겹치면 §2가 안 먹은 것이다.
2. 일주일 뒤 슬롯별 유입·체류를 보고 **성과 낮은 포맷 1개를 교체**한다.
   5종을 고정하지 말고 계속 갈아끼운다. (`config.FORMAT_SPECS` 한 곳만 고치면 된다)

---

# 구현 매핑 (2026-08-14)

조항을 프롬프트에만 적어두면 안 지켜진다. 코드가 세는 것만 실제로 지켜진다.

| 조항 | 코드 위치 | 방식 |
|---|---|---|
| §0-1 포맷 3개 초과 | `config.FORMAT_MAX_PER_DAY` | mod 5 배분이라 포맷당 2개 — 자동 충족 |
| §0-2 금지 문구 | `config.BANNED_PHRASES` → `validate.banned_phrase_hits()` / `validator.py` | 본문에서 실측, 걸리면 반려 |
| §0-3 정답 지연 | `prompts/C_body.md` | 프롬프트 지시 (자동 검출 불가 — **유일하게 코드가 못 잡는 조항**) |
| §2 도입 3줄 문형 | `FORMAT_SPECS[*].intro_test` → `validate.intro_form_error()` | 포맷별로 인용/숫자/물음표/표지어를 판정 |
| §2 도입 중복 | `config.INTRO_OVERLAP_MAX` → `validate.intro_overlap()` | 같은 날 다른 슬롯과 문자 2-gram 자카드 45% 이상이면 반려 |
| §0-4 소감 3회 | `config.SELF_VOICE_MAX` → 두 검증기 | "저는/제가/저도/저라면/제 생각" 횟수를 센다 |
| §0-5 앞 20자 완결 | `titles.head_specifics()` · `config.WEIGHTS` | 구체 요소 0이면 감점·반려 |
| §2 포맷 로테이션 | `config.SLOT_FORMATS` · `FORMAT_SPECS` → `pipeline.slot_plan()` | 슬롯이 정해 C 지시서에 주입 |
| §4-2 브릿지 2회 | `config.BRIDGE_POOL` · `BRIDGE_MIN` | 코드가 2개 골라 주입하고, 본문에 있는지 대조 |
| §4-3 스크롤 블록 | 두 검증기 | 숫자로 시작하는 줄 3개 또는 ❌/⭕ |
| §5 논쟁 질문 | `config.DEBATE_POOL` → `store.next_cta()` | 최근 글과 다른 것을 강제 로테이션 후 대조 |
| §5 하트 구걸 금지 | 두 검증기 | ❤ 가 있으면 반려 (예전엔 오히려 강제했다) |
| §6 제목 문형 | `config.SLOT_TITLE_TYPES` → `prompts/B_titles.md` | 슬롯이 정해 B 지시서에 주입 (**주입만 한다 — 아래 §11 참고**) |
| §11 문형 하루 상한 | `config.TITLE_FORM_DAILY_MAX` → `titles.form_of()` → `pipeline.stage_body()` | 상한 넘긴 문형은 확정 후보에서 제외 |
| §12 소재 5+5 | `config.SLOT_SOURCE_MODES` → `pipeline.stage_auto()` · `benchmark.py` | 슬롯이 소재 조달 경로를 정한다 |
| §6 "여배우" 6개 | `config.FEMALE_LABEL_MAX` → `validate.validate_titles()` | 10개 중 개수를 센다 |
| §7 가드레일 | `config.BANNED_HOOKS` · `EXTREME_WORDS` · `MEDIA_TERMS` | 즉시 탈락 |
| §8 분량 | `config.BODY_CHARS_MIN/MAX` | 두 경로 통일 (예전엔 서로 다른 자를 썼다) |

**슬롯 번호는 제목을 뽑기 전에 확정된다** (`pipeline.next_slot()`).
예전엔 제목을 다 뽑은 뒤에 방을 배정해서 로테이션을 걸 자리가 없었다.

---

# 추가 확정 (2026-08-17)

§0~§10 은 2026-08-14 원문이라 손대지 않았다. 아래 두 조항은 그 뒤에 확정됐다.

## §11 문형 하루 상한 — 인용 문형 하루 최대 3편

**§6 의 제목 문형 로테이션은 부탁이었다.** 슬롯이 문형을 정해 B 지시서에
넣어주기는 했지만, 확정은 `titles.score()` 가 하고 **score() 는 문형을 보지
않는다.** 선두 인용형에 `issue_lead +22` 가 붙으니 어떤 슬롯이든 인용형이 이겼다.

실측:

| 날짜 | 확정본 | 쏠린 문형 |
|---|---|---|
| 2026-08-17 | 9편 | 인용형 8편 (그중 선두 인용 6편) |
| 2026-08-16 | 9편 | "~인데" 모순형 9편 |

제목 하나하나는 세다. 그런데 홈판에 나란히 걸리면 같은 글로 보인다.
점수 함수는 **그날 가장 잘 먹는 한 문형으로 하루를 통째로 도배한다** —
0817 과 0816 은 서로 다른 문형이 도배됐을 뿐 같은 병이다.

그래서 상한을 코드가 센다.

- 판정: `titles.form_of()` → §6 의 5종 + `모순` + `기타`
- 상한: `config.TITLE_FORM_DAILY_MAX = {"인용폭로": 3}`
- 강제: `pipeline.stage_body()` — 상한을 넘긴 문형은 **확정 후보에서 제외**한다.
  점수는 그대로 둔다. 가중치를 건드리면 `titles.py` 의 실측 캘리브레이션
  (`KNOWN` 16건)이 통째로 무너진다.
- 10개가 전부 상한 문형이면 그 슬롯은 반려된다(종료 코드 4). 왕복을 줄이려고
  B 지시서에 남은 자리를 함께 넣어주지만, 그건 안내고 강제는 코드다.

지켜지는 선:

- **제3자반응(작품 관계형)은 상한 대상이 아니다.** 사용자가 3편 배정한 유형이고
  (`B_titles.md` §77), 인용구가 있어도 앞 20자에 상대역 실명이 있으면 이쪽으로
  센다. 상한에 걸리면 그 배정이 죽는다.
- **인용구를 문장 뒤로 미는 회피가 막혀 있다.** 위치가 아니라 큰따옴표 유무를
  본다. 0817 슬롯 2·8 이 정확히 그 형태(따옴표를 뒤에 둔 인용형)였다.
- 사람이 `--retitle "직접 쓴 제목"` 으로 넣은 제목은 막지 않는다. 경고만 찍는다 —
  최종 발행은 사람이 하고, 상한은 자동 확정이 쏠리는 걸 막으려고 둔 것이다.

다른 문형까지 막고 싶으면 `TITLE_FORM_DAILY_MAX` 에 한 줄 더 적으면 된다.
0816 의 모순형 도배를 막으려면 `"수치충돌": 4` 처럼. 코드는 안 고쳐도 된다.

## §12 소재 5+5 — 절반은 벤치마크에서 고른다

기존 방식은 `CELEB_POOL` 을 적어둔 순서대로 돌린다. 그 순서는 **우리가 적어둔
순서일 뿐 지금 뭐가 먹히는지와 아무 상관이 없다.** 그래서 하루 10편의 절반은
경쟁 블로그에서 실제로 좋아요를 받고 있는 인물·앵글에서 출발한다.

`config.SLOT_SOURCE_MODES` 가 슬롯별로 경로를 정한다.

| 슬롯 | 경로 | 소재를 어디서 |
|---|---|---|
| 1, 3, 5, 7, 9 | `pool` | 기존 방식 — 풀 순서 + `QUERY_SUFFIXES` |
| 2, 4, 6, 8, 10 | `bench` | `benchmark.py` — 좋아요 순 인물·앵글 |

슬롯 1 은 일일 자동 포스팅(`main.py`) 몫이고 그 경로는 이미 기존 방식이다.
그래서 v13(2~10번)은 pool 4 + bench 5 이고, **하루 전체로는 5+5** 가 된다.
홀짝으로 갈라 놓아서 포맷·제목문형 로테이션(mod 5)과 주기가 겹치지 않는다.

bench 슬롯에서 실제로 달라지는 것:

1. **인물** — `benchmark.hot_celebs()` 가 벤치마크 상위 글에 등장하는 우리 풀
   인물을 좋아요 합계 순으로 준다. `--plan` 이 두 목록을 나눠 보여준다.
2. **수집 쿼리** — `benchmark.angle_words()` 가 지금 반응 있는 앵글 3개를 뽑고,
   `crawler.collect(extra_queries=...)` 로 `"한소희 데일리룩"` 처럼 붙인다.
   인물명만 검색하면 옛 광고성 기사가 위로 오는데, 앵글을 붙이면 지금 사람들이
   보고 있는 자리의 기사가 들어온다.
3. **A 지시서** — 벤치마크 제목·좋아요·태그 10행이 함께 들어간다.

⚠️ **A 지시서의 벤치마크 블록은 근거가 아니다.** 남의 블로그 제목이다.
A 지시서 제1원칙이 "추출만 한다, 창작·보완 금지" 인데 거기 적힌 주장을 우리
팩트시트로 옮기면 그 원칙이 통째로 무너진다. 블록 자체에 "hot_materials
**정렬 지시**일 뿐이고, 같은 소재가 [소스] 에도 있을 때만 위로 올린다"고
못 박아 넣었다. `benchmark.py` 가 애초에 남의 **본문을 수집하지 않는** 것과
같은 이유다 — 유사문서 판정을 피하려면 베낄 문장 자체가 없어야 한다.

벤치마크(RSS·좋아요 API)가 죽으면 그 슬롯은 기존 방식으로 진행한다.
**대신 경고를 찍는다.** 5+5 가 실제로는 10+0 이 돼 있는데 아무도 모르는 상태가
가장 나쁘다. 실제 배분은 `state.json` 의 `source_mode` 로 사후 확인할 수 있다.
